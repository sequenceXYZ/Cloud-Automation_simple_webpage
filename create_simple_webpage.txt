In Amazon Linux:
# Install nginx on Amazon Linux:
sudo yum install nginx -y

# start nginx service:
sudo service nginx status

# start nginx service:
sudo service nginx start

# copy public IP in browser and check running webpage

# Run bash command:
sudo bash -c 'echo "<html> <body> <h1>Welcome to Accenture Cloud and Automation Bootcamp 2024</h1> </body> </html>" > /usr/share/nginx/html/index.html'

# reload nginx service:
sudo service nginx restart

# command is to configure the system so that Nginx will start automatically when the system boots up
sudo chkconfig nginx on

